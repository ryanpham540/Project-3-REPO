document.getElementById('scrollButton').addEventListener('click', function() {
    var container = document.querySelector('.horizontal-scroll-section');
    container.scrollLeft += 200; // Adjust scrollAmount as needed
   });
   